syntax = "proto3";

service Rename {
  rpc GetJsonResponse (JsonRequest) returns (JsonResponse);
}

message JsonRequest {
  int32 id = 1;
  string new_name = 1;
}

message JsonResponse {
  string response = 1;
}
